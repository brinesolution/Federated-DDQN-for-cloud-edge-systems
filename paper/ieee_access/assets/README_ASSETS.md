# IEEE Access Assets

This folder is reserved for future reproducibility scripts, metric audit files,
upload manifests, or publisher-specific assets. The current draft keeps figures
in `figures/`, tables in `tables/`, and shared manuscript constants in
`../shared/`.
